apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: developer-example-com
  namespace: default
  labels:
    app: apis
spec:
  parentRefs:
    - name: gw
      namespace: ingress-gw
      # Bind to a specific listener in the parentRef (Gateway) using sectionName field.
      # sectionName: foo
  hostnames:
    - "developer.example.com"
  rules:
    - matches:  
      - path:
          type: PathPrefix
          value: /
      backendRefs:
      # - name: httpbin-2
      #   namespace: httpbin-2
      #   port: 8000
      - group: gloo.solo.io
        kind: Upstream
        name: httpbin-3-httpbin-3-8000
        namespace: gloo-system